/**
 * Integrations: GitHub, Git, Blender, ComfyUI, FFmpeg, OpenAI, Anthropic,
 * Ollama, and the `aw` CLI sidecar bridge.
 *
 * All external communication happens here. `production-explorer/`
 * implements the filesystem IPC (tree reads) backing the Project Explorer.
 */
export {};
