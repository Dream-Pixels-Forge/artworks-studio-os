/**
 * Workspace: windows, docking, tabs, layout, navigation.
 *
 * Skeleton for Phase 0 — the docking workspace and panel system arrive in
 * Phase 1. The shell currently renders only the brand frame.
 */
export {};
